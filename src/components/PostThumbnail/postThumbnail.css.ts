import { style } from "@vanilla-extract/css";

export const dragArea = style({
    width: '100%',
    height: '300px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
});
